# i18n and Language Flow
- Language is stored in `localStorage` under `lang`, with components listening for the `app:languageChanged` event (see `src/components/AwardsSection.jsx:22`).
- The document title updates on language changes in `src/App.jsx:50`.
